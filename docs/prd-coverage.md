# PRD 覆盖情况

按模块统计 PRD 里的需求条目，以及**已经能跑并有用例锁住**的部分。
写了但没验证过的不计入——那种"完成"最后都要重做。

| 模块 | Must | 已交付 | 说明 |
| --- | ---: | ---: | --- |
| ONT 本体 | 7 | 7 | 注册表、校验、逆关系、遍历、版本化、长度上限 |
| RES 统一模型 | 12 | 12 | 12 条 Must 全部交付；FR-RES-009/010/013/014/015 是 Should，未做 |
| ARCH 架构 | 9 | 9 | FR-ARCH-011 按其验收标准交付；读路径的取消仍缺，见下 |
| IAM 权限 | 13 | 13 | 见下 |
| WF 工作流 | 9 | 9 | 见下 |
| DOM 领域 | 8 | 8 | 见下 |
| AGT Agent | 11 | 11 | 见下 |
| DASH 指标 | 9 | 9 | 见下 |
| AI 能力 | 12 | 6 | 见下 |
| CON 连接器 | 12 | 11 | 契约、网关、Browser、MCP、GitHub 已交付；见下 |
| **合计** | **102** | **95** | |

## 这份表本身有多可信

统计口径：只算 Must（M）。Should / Could 另计。**Must 的总数（102）
与每模块的条数已经用脚本对着 PRD 核过，是准的**；
`pnpm lint:coverage` 保证合计等于各行之和。

但"已交付"这一列是人写的，而它已经被发现错过三次，
错法完全一样——**把一条 Should 当成缺失的 Must 写进了备注**：

| 行 | 原来写的缺口 | 实际 |
| --- | --- | --- |
| WF | SLA 超时、可视化编辑器 | 这两条是 S 与 C。真正缺的是 FR-WF-006 外部事件触发 |
| RES | FR-RES-010 事件订阅 / Webhook 出站 | 是 S。12 条 Must 全部交付 |
| ARCH | 按 BC 重组目录 | ARCH 的 Must 里没有这一条。缺的是 FR-ARCH-011 可取消 |
| IAM | OIDC、JIT 授权 UI、委派链 | OIDC 不是编号需求；FR-IAM-006 临时授权与 FR-IAM-007 委派交集都已交付 |

这四行的备注是**照着印象写的**，不是照着需求编号核的。
核对时确认缺失的两条现在都补上了：FR-IAM-012 blastRadius
（单次 Run 的影响面上限，动手之前判）、FR-ARCH-011 可取消。

**十行里现在有九行逐条核对过编号**（WF、RES、ARCH、DASH、IAM、AGT、
DOM、AI、CON 各有细分表）。只剩 ONT 那一行还是照着印象写的 7/7。
它没有细分表，因此 `lint:coverage` 的"某行 vs 它自己的细分表"这条
对它不生效——**这一行的数字目前只有人说了算**，别拿它当已核对的。

"前两者的缺口是外部依赖，不是记错"这句话本身就是记错的，现在删掉了：
AI 那一行的九条里有六条根本不需要模型，CON 那一行的 GitHub 也不需要
真的连上 GitHub 才能验大部分。**"被外部依赖挡住"是这份表最舒服的一句话，
所以它最该被怀疑。**

**FR-ARCH-011 只按验收标准算交付，要说清楚边界。**
验收标准原文是「取消进行中的 Run，含 Connector 调用在 5 秒内全部停止」，
这一条有用例锁住：进程关停会打断在途的 Run，把 Run 迁到 Cancelled
也能被另一个进程里的运行时读到，取消之后不再写回、不再重试外部调用。

但需求正文写的是「**任何**超过 1 秒的操作接受 AbortSignal」，
而读路径（列表、图遍历、Automation Rate 的那次 N+1）目前**都不接受信号**，
客户端断开也不会停止查询。这一块没做，别把它当成做了。

核对 DOM 时抓到了这份表最坏的一种错：**代码引用了需求编号，做的却是别的事**。
`STORY_LIFECYCLE` 里写着 "FR-DOM-004"，守的是"有没有拆出任务"，
而 FR-DOM-004 说的是"必须含结构化 Acceptance"——Story 上根本没有这个字段，
本体里也没有 Acceptance 这个类型。一句写错了编号的注释比没有注释更糟：
它让人以为这条需求已经落地，于是不会有人再去看。现在它真的实现了。

写在这里而不是悄悄改掉数字，是因为这份文档唯一的用处就是可信。
一个被改对了但没人知道曾经错过的进度表，下次还会错。

### 最坏的一次：本地全绿，CI 连续四个 commit 是红的

前面几条都是"记错了"。这一条更糟：**报告出来的绿色是假的，而且假了四次。**

把 `playwright` 从 devDependencies 挪到 dependencies 时没有重新生成
`pnpm-lock.yaml`。CI 的第一步 `pnpm install --frozen-lockfile` 因此直接失败，
**一个测试都没跑到**，连续四个 commit。而本地 `pnpm check` 每次都全绿——
因为本地 `node_modules` 已经在了，install 根本不执行。

这不是运气不好。**本地的检查清单漏掉了 CI 的第一步，那本地的绿色就不代表任何事**，
而它读起来和真的一样。发现它靠的是去翻 CI 记录，不是靠任何一条检查报警——
换句话说，这个缺陷本身也是"缺信号"，和它造成的后果是同一个毛病。

修法有两部分：重新生成 lockfile，以及把 `lint:lockfile` 加进 `pnpm check`
并排在最前面。加进去之后种了一个缺陷验证过：往 package.json 里塞一个
lockfile 里没有的依赖，`pnpm check` 立刻红。

## Workflow 细分

这一行之前记的是 **7/9，缺「SLA 超时、可视化编辑器」**。那两项分别是
FR-WF-013（S）与 FR-WF-014（C），**根本不是 Must**——
也就是说这份表把缺口指错了地方，而真正缺的两条一直没被记下来。
逐条核对之后的实际情况：

| ID | 状态 |
| --- | --- |
| FR-WF-001 可配置状态机，改完即时生效 | ✅ 定义存库 + TTL 热加载 |
| FR-WF-002 guard / capability / actions / SLA | ✅ SLA 巡检会真的告警与升级；此前它只是个没人读的字段 |
| FR-WF-003 非法迁移返回可读原因 | ✅ 三种失败分开报 |
| FR-WF-004 迁移经 PDP 授权 | ✅ |
| FR-WF-005 when/if/then，8 类动作全部可用 | ✅ 8 类齐了（此前只有 3 类） |
| FR-WF-006 外部事件触发（GitHub / CI / Kafka） | ✅ 入站验签 + 翻译 → outbox，PR 合并推进 Task 有端到端用例 |
| FR-WF-007 invokeAgent 动作 | ✅ 创建 AgentRun，与人发起的走同一条路径 |
| FR-WF-008 触发链深度与频率限制 | ✅ 环形规则被熔断并留痕 |
| FR-WF-009 自动化执行全量留痕 | ✅ 含条件求值明细与"没动的原因" |

`sla` 曾经是这份表里最典型的一处虚报：它在状态定义里、在类型里、
在 Task 的 `Doing` 上都写着，**但没有任何代码读它**——
一个看起来配置好了、实际什么也不做的字段，比没有这个字段更糟，
因为有人会以为超时会有人管。现在它由 `SlaSweeper` 巡检，
超时会生成通知或升级到值班，每一条都种过缺陷验证会红。

计时用单独的 `status_since` 列而不是 `updated_at`：后者被任何一次编辑刷新，
于是一个停滞三周、昨天被人补了个描述的任务看起来"刚动过"——
而那正好是最该报警的对象。

FR-WF-006 一开始也被记成"需要真实的 GitHub"，那是错的：
**GitHub 是发送方**。它拨这个号没法在这里验证，但接的这一半可以，
而且必须——验签、认出说的是哪个对象、翻译、进 outbox、跑自动化，
整条链没有一步依赖 GitHub 真的存在。用例发的是 GitHub 真实的
`pull_request` webhook 形状，断言的是"PR 合并后 Task 变成 Done"，
也就是验收标准的原文。

翻译层把外部载荷当**不可信输入**：先验签再解析、只带声明过的字段、
原始报文不落库（里面可能有 token 和整棵 diff）。
验签用 `timingSafeEqual`——字符串比较会在第一个不同的字节上返回，
于是签名可以按响应时间一个字节一个字节地试出来。

接 CI 或 Kafka 时加一条来源配置即可，翻译层不需要改。

RES 这一行此前记的是 **11/12，缺「FR-RES-010 事件订阅 / Webhook 出站」**。
和 WF 那行是同一个毛病：FR-RES-010 是 **Should**，不是 Must。
逐条核对之后 12 条 Must 全部有用例。

顺带把 FR-RES-011 从"人工审查"变成了机器做的事。它的验收标准原文是
「端点清单审查通过」，而人工审查有个众所周知的性质：第一次做得很认真，
然后就不做了。`tests/integration/endpoint-inventory.test.ts` 把实际路由树
和一份写死的清单逐条比对——加一条路由就会红，那时必须回答
"人也走这条路吗"。种过一条 `POST /v1/agents/:id/run` 验证它会触发。

## Domain 细分

| ID | 状态 |
| --- | --- |
| FR-DOM-003 Requirement 三级 Epic/Feature/Story | ✅ `level` 枚举 + 本体校验 |
| FR-DOM-004 Story 必须含结构化 Acceptance 才能进入执行 | ✅ Acceptance 是一等对象；Ready 守卫要求 `acceptedBy` |
| FR-DOM-006 Task 的 assignee 支持 User 与 Agent | ✅ 同一套分配与权限流程 |
| FR-DOM-008 Knowledge 必须有来源引用 | ✅ Published 守卫要求 `derivedFrom` |
| FR-DOM-002 跨 BC 仅通过事件或只读引用 | ✅ 自动化规则带 `owningContext`；分层检查挡住直接写 |
| FR-DOM-001 7 个 BC 的聚合与不变量 | ✅ 见下 |
| FR-DOM-005 Dependency 环检测 | ✅ `acyclic` 写在本体里；建边时判可达性，含逆向存储的边 |
| FR-DOM-007 Release 只允许包含 Done 的 Task | ✅ `allRelatedIn` 守卫；未完成的会被指名列出 |

## IAM 细分

| ID | 状态 |
| --- | --- |
| FR-IAM-001/002/003 五层模型 · 默认拒绝 · Deny 优先 | ✅ 纯函数 PDP，逐条有用例 |
| FR-IAM-004/005 Agent 一等身份 · 默认零权限 | ✅ `ROLE_CAPABILITIES.AIAgent` 是空集 |
| FR-IAM-006 临时授权：TTL + 次数 + 绑定 Run | ✅ 三个条件都判，过期/用尽/未绑定各有用例 |
| FR-IAM-007 受限委派取权限交集 | ✅ |
| FR-IAM-008 硬性护栏不可被策略覆盖 | ✅ 在策略求值之前短路 |
| FR-IAM-010 Resource 层级继承与收紧 | ✅ 上层授权向下继承，收紧靠资源级 Deny |
| FR-IAM-012 blastRadius 熔断 | ✅ 单次 Run 的影响面上限，动手之前判 |
| FR-IAM-013 全量审计，append-only | ✅ 收回 UPDATE/DELETE + 语句级触发器，两层各有用例 |
| FR-IAM-015 多租户隔离 | ✅ PG RLS，测试以非超级用户连接 |
| FR-IAM-009 Ask：挂起 + 人工确认 + 超时拒绝 | ✅ 挂起单是领域对象；过期在**使用时**判，不靠定时任务 |
| FR-IAM-011/014 JIT 授权 UI、委派链可视化 | ❌ |

这一行第一次写成了 11/13，而上面这张细分表数出来是 12。
`lint:coverage` 现在也会核对"某一行 vs 它自己的细分表"——
补上这条检查之后立刻又抓到一处：DASH 那行写着 7，
细分表里只有 6 个 ✅（FR-DASH-001/002/003 是 ⚠️，按本表规矩不计入）。
那个多出来的 1 从这份表第一版就在。

自动保证的仍然只是**算术**：合计等于各行、各行等于细分表、
Must 条数等于 PRD。**"这条到底算不算交付"永远是人的判断**，
而那正是前面四行出错的地方。

FR-IAM-013 的注释此前是一句空话：`001_foundation.sql` 里 `audit_log`
上方写着「append-only，永不物理删除」，而同一个文件末尾的授权循环
给**每一张表**都授了 UPDATE 和 DELETE，包括它自己。
**应用能改写的审计日志不是审计日志**——它的全部价值来自
"发生过的事一定还在里面"，而这一条此前只是一行注释。

## Agent 细分

| ID | 状态 |
| --- | --- |
| FR-AGT-001/002/003 声明式定义 · 统一运行时 · 独立身份 | ✅ 一个执行入口，没有 per-Agent 分支 |
| FR-AGT-004 Context 带出处 | ✅ 每段记得自己来自哪个实体 |
| FR-AGT-007 Run 轨迹与回放 | ✅ 逐步可看 |
| FR-AGT-009/010 四种协作模式 · 不可逆操作强制确认 | ✅ |
| FR-AGT-011 经领域对象协作，无私有消息通道 | ✅ 两条 CI 规则挡住 |
| FR-AGT-012 三层预算与熔断 | ✅ |
| FR-AGT-005 四级 Memory 模型 | ✅ Working 在内存、Episodic 有界 TTL、Semantic 落 Knowledge、Procedural 即声明 |
| FR-AGT-006 Semantic Memory 必须落 Knowledge BC | ✅ 私有表上的 CHECK 让「藏语义记忆」**做不到**，而不是「不该做」 |

FR-DOM-001 的验收标准是「每个聚合有单元测试覆盖其不变量，**覆盖率 ≥ 85%**」。
现在能测了（`pnpm test` 带覆盖率门槛）：

| | 覆盖率 | 需求 |
| --- | ---: | ---: |
| Statements | 93.82% | ≥ 85% ✅ |
| Lines | 93.82% | ≥ 85% ✅ |
| Functions | 95.83% | ≥ 85% ✅ |
| Branches | 85.09% | ≥ 85% ✅ |

分支覆盖一开始是 82.58%，差 2.4 个百分点。门槛先定在 80，
因为一条从第一天就红的检查会被 `--no-coverage` 绕过去然后再没人看；
补完那些分支之后才抬到 85。**抬高的方式是补覆盖，不是调低需求。**

补的不是凑数的用例。补覆盖的过程本身抓出两个真缺陷：
脱敏在递归进 pii 命名的字段时把标记丢了（`{email:{work:"a@..."}}` 里的
地址会原样流出去），以及新增的 Risk / Milestone / Budget / Sprint / Release
没进 `contains` 的值域，压根归不到项目下。

七个上下文的聚合本身都在，不变量也都由守卫强制着
（Story 要 Acceptance、Risk 要 mitigation、Release 只装 Done 的 Task、
Knowledge 要来源、Approval 会过期），这部分是够的。

## AI 细分

这一行原本记的是「只有运行时骨架，1/12」。那个 1 对不上任何一条编号——
运行时骨架算的是 FR-AGT，不是 FR-AI。**而真正该算的四条根本不需要模型**——
它们管的是模型的**边界**，不是模型的能力。差点因为"AI 能力都被外部依赖挡住"
这句话把它们一起放过去了。

| ID | 状态 |
| --- | --- |
| FR-AI-011 指标由事件流自动物化 | ✅ 与 FR-DASH-005 是同一条：指标即查询，系统里没有写路径 |
| FR-AI-012 AI 能力可按租户 / 项目开关 | ✅ 默认**关闭**；忘了配置就什么都出不去 |
| FR-AI-013 提示注入防护 | ✅ 上下文围栏 + 冒充系统的片段被中和并留痕 |
| FR-AI-014 数据出境控制 | ✅ 供应商白名单、分级上限、PII 脱敏、全量审计（ADR-0006） |
| FR-AI-001 自动生成 WBS（Draft 模式，逐节点可操作） | ✅ 提议是一个对象一条，可逐条接受 / 拒绝 |
| FR-AI-002 拆分覆盖父需求验收标准 ≥ 95%，未覆盖项列出 | ⚠️ 口径已实现并验证，**生成侧待模型**，不计入 |
| FR-AI-004 生成 ≥ 2 个 Sprint 候选，不违反依赖与 capacity | ⚠️ 可行性校验已实现并验证，**生成侧待模型**，不计入 |
| FR-AI-006 风险可点击到触发它的实体 | ✅ Risk 生命周期守卫要求 `evidencedBy` 关系；对人建的风险一样生效 |
| FR-AI-007 无出处的内容标注「待确认」 | ⚠️ 标注与复查已实现并验证，**成文侧待模型**，不计入 |
| FR-AI-009 推荐带出处与时效 | ⚠️ 扣下规则已实现并验证，**推荐侧待模型**，不计入 |
| FR-AI-010 行动项一键创建并关联负责人 | ⚠️ 负责人规则已实现并验证，**纪要抽取侧待模型**，不计入 |
| FR-AI-003 估点中位偏差 ≤ 1 档 | ❌ 要真实模型 + 三个迭代的历史样本 |

FR-AI-001 是这一行里第二次纠正判断。它的验收标准是
「生成对象树并**可逐节点接受/拒绝**」——那是**机制**，不是模型输出质量。
和整个 Agent 运行时一样，用确定性模型就能完整验证（运行时 11/11 就是这么测的）。
把"需要模型才能产出好内容"和"没有模型就做不了也验不了"混为一谈，
是我先前把这一整块记成 ❌ 的原因。

改动本身：Draft 模式不再直接创建目标对象，而是一个节点落一条 `Proposal`。
接受时才真的创建，走的是普通 `create`，所以本体校验、权限、审计、事件一个不少——
**Agent 提议的对象和人建的对象没有区别**，只是多了一步谁来点头。
原先那条"Draft 直接落库"的用例因此红了，那正是这次改动的意义：
只能对整棵树表态的话，实际结果是全都不要。

这一段上一版写的是「其余几条按理也能验证，本轮没排上」，然后我把
002 / 004 / 007 / 009 / 010 一律记成了 ✅。**那是虚报，现在改回来了。**

写出来的是它们的**验收口径**——覆盖率、依赖与 capacity、出处标注、
时效、负责人——一组纯函数，有用例、过了变异测试（19/19）。
但这些需求要的是**能力**（拆需求、排 sprint、写 PRD、推荐知识、抽行动项），
而能力要模型。**把"验收标准可判定"当成"需求已交付"，是这份表犯过的
同一类错的新变种**：前几次是把 Should 当 Must，这次是把尺子当成了被量的东西。

所以它们记 ⚠️，按本表规矩不计入。口径留着，等生成侧接上时它就是现成的闸。

FR-AI-006 是这一批里唯一真正交付的，而它的第一版实现也是错的，
值得完整记一笔：

- 第一版是 Agent 运行时里的一个产出闸，检查 `attributes.evidence` 空不空。
- Risk 的本体里**根本没有这个属性**，于是那个闸会拒掉每一条风险提议——
  一个把功能彻底关掉的"校验"。
- 就算加上，一个字符串数组也**点不动**。「可点击到实体」在这个系统里
  的意思是**关系**。
- 现在它是 Risk 生命周期上的 `hasRelation: evidencedBy` 守卫，
  和 Knowledge 的来源守卫（FR-DOM-008）同一套机制。好处不只是对：
  它对**所有**风险生效，Agent 建的和人建的一视同仁。

闸设在 `Mitigating` 而不是 `Identified`：刚发现时说不清出处是正常的，
设在入口会让人不敢登记风险，而**漏登记比登记得潦草糟得多**。
但要动手缓解、要占用人和时间，就得说清楚是什么让你这么判断的。

单开 `evidencedBy` 而不复用 `derivedFrom`，是因为复用要把后者的值域一起
放宽，那会**顺手改掉 Knowledge 的不变量**——一次为了少写十行做的语义变更，
事后没人记得是这么来的。

`requireEvidence` 那个校验函数因此删了。**建了之后发现正确实现在别处，
该走的是它，不是让它留在那儿**——留着就成了下一个"看起来有人管"的东西。

FR-AI-003（估点中位偏差 ≤ 1 档）和上面那五条 ⚠️ 是同一类：要模型。
区别只在 003 连口径都还没写——它的口径要历史样本才定得下来。

FR-AI-013 要说清楚主防线在哪：**结构性的那一层**——模型的动作空间是一个
封闭联合（finish / propose / call），能提议什么由 Agent 声明的 `mayPropose`
限死，能调什么工具由连接器白名单限死。注入成功也拿不到新权限。
文本中和是第二层，防的是"检索来的内容冒充系统在说话"：
即使拿不到权限，一段写着 `### system:` 的需求描述也足以让模型误解自己的角色。

## Dashboard 细分

| ID | 状态 |
| --- | --- |
| FR-DASH-005 指标由事件流自动物化，无人工填报 | ✅ 指标即查询，系统里不存在写路径 |
| FR-DASH-006 可下钻至实体清单 | ✅ 与指标共用 filter，条数必然一致 |
| FR-DASH-010 遵守资源级权限 | ✅ 走 `service.countGrouped`，与列表同一次授权 |
| FR-DASH-011 新鲜度 ≤ 1 分钟 | ✅ 现算，延迟为 0 |
| FR-DASH-015 自动化分级与 Automation Rate | ✅ 口径表逐行对着 §2 写了用例；分级明细随指标一起返回 |
| FR-DASH-003 Agent 视角 8 项 | ✅ 成本 / Token / 成功率 / 采纳率 / 时延 / Ask 率在目录里，Automation Rate 与 Rework Rate 走北极星路径 |
| FR-DASH-001 Project 视角 8 项 | ✅ Burn Down / Velocity / Milestone / Risk / Budget / Scope Change / Cycle Time / Lead Time |
| FR-DASH-002 Team 视角 6 项 | ✅ Capacity / Workload / Review Time / WIP / Blocked / Human vs Agent |
| FR-DASH-016 7 天推翻窗口的回溯修正 | ✅ 重开成为一条显式的边（[ADR-0012](adr/0012-reopen-is-an-explicit-edge.md)）；历史值不存，因此天然随事实修正 |
| FR-DASH-007/008/012 异常检测、归因、重放 | ❌ |

「指标即查询」的代价要说清楚：现算在百万行规模上没问题
（[性能基线](perf-baseline.md)里列表查询 P95 约 20ms），
再大一个量级就需要物化。届时这份指标定义就是物化任务的输入，
不需要重写口径。

Agent 视角凑齐 8 项时给指标模型加了**求和 / 求平均 / 比率**——
数条数答不了"花了多少钱"。加的是动作种类，不是自由度：
仍然只接受属性名，不接受 `sum(cost * 1.2 + fee)` 这类表达式。
一旦允许表达式，指标就不再是"能被读懂、被复算、被质疑"的东西了。

补齐 Project 与 Team 视角时，差的不是代码而是**对象**：
Velocity 要有 Sprint，Milestone / Risk / Budget 各自要有类型。
先建类型再补指标是对的顺序——反过来做只会得到几个恒为零的数字，
而恒为零的指标比没有指标更容易让人误判。这四个类型按 PRD 03 的
关键属性与不变量建出来了，其中 Risk「高风险必须有 owner 与 mitigation」
做成了 `Mitigating` 状态的守卫：登记一条风险不该有门槛，
但声称"正在缓解"就必须说得出对策。

Cycle Time 与 Lead Time 目前只给出**计数**，没给时长分布。
`status_since` 已经在库里（007_sla.sql），把它展开成分布是下一步——
现在的口径写在指标定义里，没有假装它是分布。

**Automation Rate 是唯一的例外，它明显更贵**：每个工作项都要拉一次历史
再算一次编辑距离，是一次 N+1。目前用一条 5,000 项的硬上界兜住，
命中时响应里的 `truncated` 为 true——**少算可以，不声不响地少算不行**。
这一条是已知的、要还的债，不是"没想到"。

## 差得最远的两块

**AI 能力（12 Must，交付 6）。** 这一行改过四次，前三次都是同一种误判
（把"需要模型才能产出好内容"当成"没有模型就做不了也验不了"），
第四次是**反方向过头了**：把验收口径写出来就记成需求交付。

真正交付的六条都不涉及模型的产出质量：开关、出境控制、注入防护、
指标物化、逐节点可接受/拒绝的提议、风险必须指得出证据实体。

另外六条要模型（002/003/004/007/009/010）。其中五条的**验收口径已经
写成可执行的规则并验过**，等生成侧接上就是现成的闸；但口径不是能力，
所以不计入。**没有凭据时 runner 默认不启动**：退回到假实现会安静地
产出一堆无意义的草稿，而草稿是会被人当真的。

**连接器（12 Must，交付 11）。** 统一契约与网关做完了——授权、审计、幂等、
限流、退避重试、熔断、白名单、人工确认、凭据注入、按分级脱敏，
全部在**唯一入口**上强制执行，每一条都种过缺陷验证会红。
FR-CON-002「无法绕过」用两条 CI 规则挡住（依赖分析 + 源码扫 `fetch`），
两条都验证过会触发。

缺的是具体集成，而**阻塞理由不一样**，不该混作一谈：

| ID | 状态 |
| --- | --- |
| FR-CON-001 统一契约（Read/Write/Observe/Subscribe/Action） | ✅ |
| FR-CON-002 Agent 无法绕过 Connector 直连外部系统 | ✅ 两条 CI 规则（依赖分析 + 源码扫 `fetch`），都验证过会触发 |
| FR-CON-003 每次调用先经 PDP 授权并审计 | ✅ |
| FR-CON-004 写操作幂等 | ✅ 缺幂等键的写直接拒 |
| FR-CON-005 限流、指数退避重试、熔断 | ✅ |
| FR-CON-009 Browser：域名白名单 + 动作范围 + TTL | ✅ 真实 Chromium 端到端验证 |
| FR-CON-010 Browser 敏感动作强制人工确认 | ✅ `submit` 显式标注，未确认时阻断 |
| FR-CON-011 凭据由 Secret Manager 注入，上下文无明文 | ✅ |
| FR-CON-012 数据分级与字段脱敏 | ✅ |
| FR-CON-006 GitHub：PR / Commit / Review / CI | ✅ 三段验收标准分别验；成色见下 |
| FR-CON-007 Jira | ❌ 被外部依赖挡住：需要真实实例，且要双向同步 |
| FR-CON-008 MCP：接入任意 MCP Server 作为 Tool | ✅ stdio JSON-RPC 全程往返，对面是一个真实的本地 server |

这张细分表是逐条列出来才发现的：CON 原本记的是 8，而按编号数是 7
（加上 Browser 那两条才是 9）。和 AI 那一行一样，多出来的那个 1
从来对不上任何编号。`lint:coverage` 现在会比对"某一行 vs 它自己的细分表"，
所以这类漂移当场就红。

把这些一律写成"被外部依赖挡住"是不诚实的——**手边有的东西不算阻塞**。
Browser 就是例子：Chromium 一直在这台机器上，写完之后端到端跑通了，
三条控制（白名单、动作范围、人工确认）各自种过缺陷验证会红。
MCP 也做完了：不引第三方 SDK，自己实现 initialize / tools/list / tools/call
三个方法，对面跑的是一个真实的 stdio server（`tests/fixtures/mcp-server.mjs`）。
用假 transport 的话，分帧、id 匹配、通知跳过、错误映射这几处
恰恰最容易错又完全测不到——把"通知当成回复"种进去之后，
14 条用例红了 8 条。

「接入**任意** MCP Server」这句话的代价要写清楚：server 是别人写的代码，
它的声明来自系统外面。所以每个默认值都倒向保守一侧——
工具不默认可用（白名单之外一个都调不了）、
**不默认只读**（没有显式 `readOnlyHint` 的一律当 action，因而走人工确认）、
返回内容一律按 confidential 脱敏。反过来设的话，
接一个新 server 就等于把它声明的一切直接授权给 Agent。

Browser 连接器落地时顺带抓出三个真问题：
白名单按 `host` 比对而 host 含端口，于是 `api.example.com` 配不上
`https://api.example.com:8443/…`；参数校验排在开浏览器之后，
一次少写 selector 的调用要先花几百毫秒拉起 Chromium 才报错；
以及 `src/` 依赖了一个 devDependency——分层检查当场拦下，
那正是它存在的理由。

契约已经把它们变成薄薄一层：实现 `Connector` 接口，声明操作与白名单即可，
授权、审计、幂等、限流、退避、熔断、脱敏全部由网关提供。

### FR-CON-006 的成色，说清楚

验收标准是「可创建 PR、读取 CI 状态并回写 Task」，三段分别是这么验的：

| 这一段 | 怎么验的 | 需要 GitHub 在场吗 |
| --- | --- | --- |
| 创建 PR | 本地 HTTP 服务器逐字断言方法、路径、版本头、报文 | 否 |
| 读 CI 状态 | 报文是**从本仓库自己的 CI 真实抓下来的** | 否 |
| 回写 Task | 真数据库、真生命周期守卫、真审计 | 否 |

**没有覆盖的是：github.com 会不会接受这些请求。** 拿不到产品自己的凭据
（会话里那个 token 属于这次会话，不属于 ProjectOS——用它写出来的用例
只在这个沙箱里绿），这一条就验不了。所以这个连接器和 MCP（对面是真 server）、
Browser（对面是真 Chromium）**不是同一个成色**，别当成一样的。

fixture 用真抓的而不是手写的，是因为手写 fixture 等于把"我以为它长这样"
当成事实来测。真抓的那份里有一条 `in_progress`——手写的话大概率只会写
success 和 failure 两种，而"还在跑"恰恰是判断逻辑里最容易错的一档。

三个值得记的决定：

- **白名单的单位是 repo，不是 host。** 按 host 的话，"允许访问
  api.github.com" 等于把账号下每个仓库都授权出去了——那不是任何人
  想批准的那件事。
- **GitHub 的九种 conclusion 收敛成三种**（passing / failing / pending），
  且用白名单：未知的结论一律算失败。GitHub 将来加一种时，**误报比漏报安全**。
- **CI 绿了不自动推进任务**。CI 通过只说明代码能编译能跑测试，不说明这个
  任务做完了。自动推进会让"完成"这个信号变得不可信，而那是看板上最重要的信号。
  唯一的例外是解除**它自己打的**那个阻塞——而"自己打的"要真的查一下
  blockReason 的前缀：一个因为"等第三方接口"被人挂起的任务，
  不该因为 CI 变绿就被放回去。

变异测试 20/20。第一轮 18/20，两条活下来的都是用例太弱：
一条把阻塞原因断言成"含 CI 两个字"，于是把原因换成光秃秃的「CI 失败：」
照样通过——而那对着手修的人一点用没有；另一条只测了"出发前就已取消"，
没测**已经在等**的那次，而验收标准要的正是后者。

## 已经不打算按原样做的

无。目前所有偏离都记在 [ADR](adr/) 或[自用日志](dogfooding-log.md)里，
没有被静默跳过的需求。
